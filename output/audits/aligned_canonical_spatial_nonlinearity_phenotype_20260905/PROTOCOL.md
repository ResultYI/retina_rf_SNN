# Phase1: aligned Canonical spatial-contrast residual phenotype

This is architecture-development analysis, not a confirmatory mechanism test. Only Phase1 is authorized. No training, parameter fitting, model/checkpoint/production changes, new model or statistical seeds, RF/temporal audit, synthetic/illusion probes or rectification implementation. All22 biological cells remain included. Zero-center is not needed and will not enter the primary decision.

## Frozen identity and data

Resolve aligned22 final checkpoint paths from the preceding macaque_aligned_heldout_pathway_evaluation_20260905/evidence_manifest.json checkpoint_identity entries. LN/CNN final artifacts come from the same manifest. Record hashes, branch, HEAD, dirty git status and unchanged original data/source hashes. Public Canonical V1 revision4: h1-shared-bc-direct-broad-ac; bc-central-disk_ac-overlapping-full-disk. Fixed LN-derived centers,33 trainable scalars, same final fresh full-train refit. No calibration biases from the subsequent bias-control task are used.

Primary development data is original validation live [16,20) s, live frames[2400,3000), decoded[3151,3751), segments16..19. Preserve all22 cells/37 recordings/137 recording-local trials:548 sequences,65760 scored bins. Native150Hz;150-bin sequence reset; first30 unscored; score30..149; same calibrated17x17 L+M Weber drive from original51-pixel crop/3-pixel pooling; original event target1[count>0], strictly-past observed event history, recording/local/global trial order. Decoded live origin751 remains acquisition-provenance UNKNOWN. Bounds zero-based half-open.

Before phenotype analysis, strict-load each aligned/LN/CNN final checkpoint, rebuild original validation via production loader, require exact input/target/mask/source/trial order, bitwise saved logits and exact original NLL for all66 cell/model pairs. Canonical/LN CPU Torch2.6.0+cpu threads2, batch8; CNN original deterministic GPU Torch2.10.0+cu126/RTX4070 Laptop, noAMP/TF32, same batch8, followed by original CPU NLL. Any mismatch stops. No conversion or numerical tolerance relaxation. Constructor seeds are the already-existing seeds only, with strict frozen state loading, not new seeds.

## Unique primary stimulus-only definition

I[t,x] is the unmodified front-end L+M Weber drive, before any model H1/BC transformation. For each aligned checkpoint use exactly model['feature_bank.bc_support'][0], the existing fixed binary BC central disk. Source support_partition.py fixes radius0.06degrees for midget/PC and0.10degrees for parasol/MC, centered at that checkpoint's frozen cell_positions_degs. Use w[x]=support[x]/sum(support). Weights are nonnegative, sum1, fixed, uniform within this support and zero outside. Do not use AC support, learned mixture weights, output-derived RF or fitted amplitudes. Record support/weight/coordinate hashes and sample count for every cell (current supports contain3–13 front-end spatial samples).

M[t]=sum(w I); LSC[t]=sqrt(sum(w*(I-M)^2)); localRMS[t]=sqrt(sum(w I^2)). Float64 feature arithmetic; all quantities have the dimensionless calibrated Weber-drive unit. M is signed local mean drive, not a physical luminance estimate. This measures spatial heterogeneity in the model's fixed direct-BC integration neighborhood; it is a coarse front-end neighborhood statistic, not anatomical reconstruction. No target, loss or model output defines w or any feature. Stop if a support is invalid/empty or stimulus-only primary strata cannot be formed; never switch metric/support.

## Stimulus-only thresholds and matched control

For each cell use all development scored rows, with their existing recording/trial multiplicities, to set LSC quantiles at0.2,0.4,0.6,0.8 by NumPy linear quantile. Q=1+searchsorted(edges,value,side='right'), so boundary ties go to the upper interval. Require strictly increasing edges and nonempty Q1..Q5; record actual counts rather than claim exact20% when ties occur. No tie-breaking with targets/loss and no alternate cutoffs. Save/hash-lock feature tensors and thresholds before accessing per-bin losses.

Local mean M uses the same five fixed development quantile edges and upper-on-tie rule. Mean-matched E compares the SAME global LSC Q5 versus Q1 inside each of the five M strata. A stratum contributes only if both groups are present. Let a_s=min(n_Q1,s,n_Q5,s), and normalized weight alpha_s=a_s/sum(a_s). Matched E=sum(alpha_s*(mean(D|Q5,s)-mean(D|Q1,s))). This outcome-free overlap weighting balances the discrete M-stratum distribution without random sampling or parameter fitting. Save all stratum counts, retained strata, overlap mass, weights, and actual mean M in each LSC group. If a cell has no common-support stratum, STOP the primary definition instead of changing bins/excluding that cell. The match is coarse quintile stratification, not exact continuous-mean matching; disclose remaining within-stratum M differences. No alternate mean bins or weights after results.

## Frozen loss, statistics and controls

On identical scored rows compute float64 Bernoulli contribution softplus(z)-y*z, treating saved float32 logits as exact inputs. Report maximum numerical difference from original float32 NLL, which is separately replayed exactly. Models: aligned Canonical, LN, CNN. D_LN=ell_Canonical-ell_LN, D_CNN=ell_Canonical-ell_CNN. Positive means Canonical has higher loss. Primary E for each comparator is mean(D|Q5)-mean(D|Q1), not a difference of separately selected data. Report every quantile's three model NLLs, both relative losses, bin count, event rate and M/LSC descriptors. Report Q1/Q5 event rates, but do not fit or match an outcome-based rate model. Absolute Canonical difficulty alone cannot support GO.

Population uses equal biological-cell weights,22 complete cells. Report mean/median E, positive/negative/exact-zero counts, mean and median95% percentile paired-cell bootstrap intervals for raw and mean-matched E. Reuse the already-existing statistical seed2026090501 and its100000-draw22-column index matrix (no new seed); numpy.default_rng, linear2.5/97.5 percentiles. Pairing preserves Q5/Q1 and comparator/matched values within each cell. No p-values, no treating bins/trials as independent biological replicates.

Secondary continuous trends: per-cell Spearman(LSC,D_LN/D_CNN) using average ranks for ties; no Pearson is added. Describe the22-cell correlation distribution only. MC_ON/MC_OFF/PC_ON/PC_OFF summaries: cell count, mean and median raw/matched E, with no group tests or subset selection. Sole robustness check: for each of the two raw primary E vectors, remove the single cell with maximal absolute E (lexical cell_id tie-break) and recompute mean once. No additional deletions, matched-outlier diagnostic or robustness analyses.

## Development-only decision fixed before results

For at least one comparator, GO requires all of: raw mean E>0 and its mean95% interval strictly above0; single-deletion mean remains>0; matched mean95% interval strictly above0; matched mean E retains at least50% of that comparator's raw mean. The50% rule operationalizes substantial retention before results, not a new significance test. Both comparators need not pass; disclose disagreement. Label: GO — SPATIAL-NONLINEARITY PHENOTYPE SUPPORTED. This supports a contrast-associated relative residual phenotype, not proof of BC rectification or authorization to implement Phase2.

If GO does not hold but at least one comparator has both raw mean E>0 and matched mean E>0, label MIXED — WEAK PHENOTYPE (including wide intervals, strong attenuation, class heterogeneity or one-cell dominance). Otherwise label NO-GO — NO EVIDENCE FOR THIS MODEL DEFICIT; also state NO-GO FOR BC RECTIFICATION. No result-driven metric/support/cutoff/subset changes. Return to user/Chat for any Phase2 decision even under GO.

## Previously consumed range: gated descriptive analysis

Complete ALL development analyses above and write/hash-lock development_verdict.json BEFORE reading any consumed-range phenotype features, losses or contrasts. Then, once, analyze live[20,60),frames[3000,9000),decoded[3751,9751),same22/37/137 and657600 scored bins, with the same support/feature rules, FROZEN DEVELOPMENT LSC AND M THRESHOLDS, matched weighting formula and relative-loss metrics. Q1/Q5 retain the development cutoffs and need not contain20% each in this range. No recomputed consumed thresholds. Only descriptive means/medians/counts and direction comparison; no new bootstrap or outlier test on this secondary set. If a secondary matched cell lacks overlap, record matched estimate unavailable for that comparison (no subset population mean) without altering development decision. Secondary results cannot rescue or reverse GO/MIXED/NO-GO. This is previously consumed descriptive reuse, not independent confirmation. Initial hashing/identity checks of prior artifacts are not a residual-vs-contrast analysis.

## Outputs and boundary

Save compressed per-bin feature/target/loss rows with split/cell/recording/trial/live-frame/time identity; feature thresholds and hashes; quantile_nll.csv; per_cell_excess_loss.csv; population_excess_loss.csv; mean_matched_control.csv; cell_group_summary.csv; development_vs_consumed_descriptive.csv; eight-answer REPORT and evidence_manifest.json. Preserve old artifacts and dirty work. No additional experiments, support variants, rate model, retraining or rectification. Stop after delivering Phase1.
