# Context review — Canonical V1 correctness patch

Status: **PASS** for the four requested implementation-contract failures. No missed in-scope requirement or unintended production callsite regression was found.

Scope: read-only source/audit/callsite review. No production or test edits, training, optimizer steps, new scientific experiments, web access, or git-history investigation. This file is the sole output of the reviewer. Existing execution artifacts were inspected; they were not rerun by this work unit.

## Explicit scope limitation

A manually altered current-marker checkpoint can retain complete masks while replacing the broad spatial encoder values independently of the direct encoder. This is a nonblocking limitation, not one of the four original reachable configuration failures.

At `models/mechanistic_retina/spatial_contract.py:82-91`, `_check_loaded_geometry` checks only the allowed positive-value pattern and equality within paths 0/1 and within paths 2/3. It does not check that direct and broad path values are the corresponding masked/normalized views of the same `feature_bank.spatial_basis`.

Concrete source-level counterexample: retain a valid current state, including its spatial/causal markers and both full-disk support buffers, then multiply only `feature_bank.path_spatial_basis[:, 2:]` by 2. Shapes, finiteness, nonnegativity, nonzero pattern, 0/1 equality, and 2/3 equality all remain true. The pre-hook therefore accepts the state under either strict setting. `bipolar_subunits.py:150-155` uses those modified values directly in `basis_kernels`, and `model.py:145-158` passes their resulting broad activity into AC. This permits a second view-specific encoder scale through the checkpoint, although the accepted shared-BC contract allows the view difference only through support. The current construction behavior is explicitly locked by `tests/test_causal_pathway_contract.py:73-96`, which derives both views from the same spatial basis and normalizes them on their supports.

The new geometry-load test at `tests/test_canonical_geometry_entry_correctness.py:181-204` covers mask holes, path-only holes, out-of-support values, and a missing basis, but not positive in-support value corruption. The task owner explicitly adjudicated stored coefficient normalization/scaling validation as outside the four mask/support/legacy findings. The original custom-mask hole and its current-marker load escape are closed; production construction still creates both normalized views from the same spatial basis. No hostile-checkpoint integrity audit, new guard, or additional experiment is required in this task. Accordingly, this limitation does not block the four-case PASS.

## Original counterexample coverage

| Original evidence | Current coverage and source result |
|---|---|
| `output/architecture_conformance_20260831/runtime_probe.py`: N=6, E=10, seed 831, two self-only rows, direct/broad derivatives outside the target disk | `tests/test_canonical_mixing_correctness.py:16-64` preserves the exact cell/cone geometry and groups and checks every cell/component through production forward. Its stimulus/history are simplified; `original_runtime_after.json` separately retains the original probe fixture and reports both outside-mask maxima exactly 0 with empty outside-cone lists. |
| Original minimal two-cell example with positions/cones 0 and .07 | `test_canonical_mixing_correctness.py:67-97` retains the exact geometry, a positive cross-cell coefficient, nonzero inside derivative, and exact-zero outside derivative. |
| `spatial_dead_parameters_probe.py`: BC=[1,0,0], AC=[1,0,1] at cone positions 0,.04,.12 | `test_canonical_geometry_entry_correctness.py:35-62` contains the same hole and first three positions, padded by one outside-disk cone at .16. The unchanged full-disk constructor values are required exactly by `pathway_spatial_geometry.py:69-75`; valid full-disk control is at test line 65. |
| `spatial_dead_parameters_probe.py`: four isolated class/polarity groups, self-only trainable raw connections | `test_canonical_mixing_correctness.py:100-126` preserves the topology, excludes all mixer parameters from the optimizer, checks fixed identity and exact logit invariance. Mixed-degree and original N6 counts are checked at lines 129-149 and 191-201. `mixing_parameter_counts.json` records N6 88→86 and isolated N4 80→76 trainable scalars. |
| `legacy/legacy_in_memory_probe.sh`: explicit legacy config is accepted by validator, builder, strict state load, and forward | `test_canonical_geometry_entry_correctness.py:96-175` checks the constructor and public builder, a complete current payload mutated to legacy, absent identity, both strict settings, rejection before copying response-bias state, and current payload acceptance. `canonical_contract.py:12-20` gates the constructor before components are built; `karamanlis_v1_rf_validation.py:60-94` validates serialized identity. The new cases reconstruct the violating identity rather than preserve the old 50-key legacy payload verbatim. |

Initial red evidence is retained in `mixing_red.txt`, `entry/red_reference.json`, and `entry/geometry_load_red_tool_result.json`. Existing final regression XML records 101 tests, zero failures/errors/skips. These artifacts corroborate the in-scope covered cases. They do not establish arbitrary checkpoint coefficient integrity, which remains outside scope.

## Callsite and frozen-contract review

- The only production `feature_bank(...)` callsites are `models/mechanistic_retina/model.py:145` and `models/mechanistic_retina/pathway_rf.py:22`; both pass the shared mixer. For N>1, `bipolar_subunits.py:170-177` mixes kernels, applies the target support, then contracts cones. N=1 retains contraction then mixer order. No stale production caller was found.
- The RF helper continues to derive final/current RFs from production forward; basis terms use the same corrected feature path. Existing tests with the older direct primitive composition remain primitive checks, not production callsites.
- `data/karamanlis_rf_artifact.py:120-159` still produces contour-derived masks. The formal Karamanlis RF/AC builders continue passing this geometry to the Canonical builder, so incompatible contours now fail explicitly; no contour conversion or radius adjustment was added. Exact real contour compatibility remains UNVERIFIED in this work unit, consistent with the original audit boundary.
- Changed/new production files are `bipolar_subunits.py`, `canonical_contract.py`, `model.py`, `pathway_rf.py`, `pathway_spatial_geometry.py`, `shared_subunits.py`, `spatial_contract.py`, `support_partition.py`, and `evaluation/mechanistic_retina/karamanlis_v1_rf_validation.py`. The finite-coordinate check in support construction directly prevents nonfinite cones being silently interpreted as outside the same prescribed disks; it does not change a radius or physiological parameter.
- No schema/revision rename, architecture routing redesign, radius value change, temporal-bound change, loss/training change, or physiology mutation was found in the bounded snapshot/source comparison. Revision remains 4 and the current causal/spatial identity strings are retained. Explicit legacy entry rejection is intentional, with no silent conversion.
- The lineage summary reports 22 strict loads with unchanged state identity/trainability and 1,188 bitwise tensor comparisons. This reviewer inspected that summary but did not repeat checkpoint replay; the independent lineage work unit owns that execution evidence.

