# Execution evidence

Runtime: D:/anaconda/python.exe, PyTorch 2.6.0 CPU, two threads. No debugger, environment changes, temporary production instrumentation, training or optimizer.

The first inference-only run reproduced all 22 saved aligned 16-bin RF tensors exactly from complete 150-bin Jacobians, then stopped at fresh seed 67#4/fresh_1 with `Unsupported global: models.mechanistic_retina.contracts.ArchitectureMode` under `weights_only=True`. The primary replay gate was unaffected. No inference-results artifact had yet been written.

Loader hypotheses: (1) Enum metadata needs a narrow safe-global allowlist; inspect unsafe globals and toggle only this allowlist. (2) Corrupt checkpoint; compare frozen SHA256 and archive CRC. (3) Wrong artifact/schema; compare the actual seed producer and loaded metadata with the requested cell/stage. No production file will be changed. The intended sole loader edit is a context-managed allowlist for the already inspected local ArchitectureMode enum. Execution logs and this note are retained as requested audit evidence.

Observed toggle: frozen hash identity `True`; archive bad CRC member `None`; unsafe globals `['models.mechanistic_retina.contracts.ArchitectureMode']`. Without allowlist: `UnpicklingError`; with only ArchitectureMode: loaded `67#4 trained h1-shared-bc-direct-broad-ac`; removing allowlist: `UnpicklingError` again. The loader now scopes this enum via `torch.serialization.safe_globals`, retaining `weights_only=True` and strict model loading. No checkpoint conversion. No temporary files outside this output directory or debug processes were created.

The subsequent synthetic full-forward equality check failed. Hypotheses checked: thread-dependent numerical reduction (rejected: identical maximum error at 1/2/2 threads); source drift (seven producer hashes differ, all listed in the existing multi-cell correctness patch); history/state input mismatch (zero observed history and saved validation cones; H1 state exact). Teacher current BC direct maximum error=0.045529864728450775 and probability maximum error=0.011970221996307373. No source repair or old-code monkeypatch was attempted. The report retains this as failed current synthetic full-forward replay. Historical reconstruction from saved post-gain currents through hash-identical RGC/state code passed exact teacher probabilities, stored KL and all saved intervention logit deltas. Protocol amendment preceded reconstruction.

Final inference run: 22 real full150 RF checks, four full600 diagnostics, twelve existing seed RF/value diagnostics and four historical synthetic current reconstructions completed. The `all_passed` field in inference-verification.json refers to its real-model `checks` array and reconstruction completion; the separate `synthetic_current_full_forward_exact=false` flag must not be omitted. Training remains zero. One statistics invocation before inference completion exited on missing inference-results.pt; it produced no tables. The final statistics invocation completed successfully.

The RF energy-centroid displacement accumulation was corrected from an intermediate float32 coordinate tensor to float64, consistent with the predeclared statistic. Only this audit's new CSV was updated from its stored double-precision centers; no inference or historical artifact changed. All values are independently checked again during finalization. No temporary instrumentation remains.
