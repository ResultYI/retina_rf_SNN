Independent read-only review corrected the binding of the predefined numerical tolerance: paired clamp-minus-normal uses the sum of normal and clamp tolerances; the asymmetric negative control gets its own tolerance and must exceed it. The reference cell/pair and history schedules did not change. Primary shared-history comparisons did not change. Difference-of-differences for every saved cell will also be independently recomputed from NPZ arrays under the corrected formula, without rerunning model inference. Secondary peak-index comparison is restricted to the already defined post-onset interval.
